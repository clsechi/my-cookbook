<!DOCTYPE html>
<html>
  <head>
    <title>MyCookbook</title>
    <%= csrf_meta_tags %>

    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    
    <link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet">

    <%= stylesheet_link_tag "/css/custom.css" %>

  </head>

  <body>
  	<!-- navigation -->
  	<nav class="navbar navbar-toggleable-md navbar-default">
  		<div class="container-fluid">
  			<div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span> 
          </button>
          <a class="navbar-brand" href="/">CookBook</a>
        </div>
        <div class="collapse navbar-collapse" id="myNavbar">
          <ul class="nav navbar-nav">
            <%= render partial: "/partials/search_form" %>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <li><a href="/">Início</a></li>
            <li><a href="#">Minhas Receitas</a></li>
            <li><%= link_to "Enviar uma receita", new_recipe_path %></li>          
            <% if user_signed_in? %>
            <li><p class="navbar-text">Bem-vindo <%= current_user.email %></p></li>
            <li><%= button_to "Sair", destroy_user_session_path, :method => :delete, class: "btn btn-success navbar-btn"   %></li>
            <% else %>
            <li><%= button_to "Entrar", new_user_session_path, class: "btn btn-success navbar-btn" %></li>
            <% end %>
          </ul>
        </div>        
      </div>
    </nav>

    <% if flash[:notice] %>
      <div class="alert alert-success notice">
        <%= flash[:notice] %>
      </div>
    <% end %>

    <% if flash[:alert] %>
      <div class="alert alert-danger alert">
        <%= flash[:alert] %>
      </div>
    <% end %>

    <%= yield %>

    <!-- footer -->
  	<div class="text-center center-block footer">
  		<p class="font-weight-bold"> CookBook | Copyright 2018 </p>
  		<a href="#"><i class="fa fa-facebook-official fa-3x"></i></a>
  		<a href="#"><i class="fa fa-twitter fa-3x social"></i></a>
  	</div>
  </body>
</html>
